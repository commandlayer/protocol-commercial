# Execution Model (Draft)

This document will describe:

- How x402 entries map into endpoints/*/route.json
- How runtimes discover the correct handler for a verb + version
- How Protocol Commons schemas are enforced at runtime
- How Commercial verbs extend the economic surface area (checkout, verify, etc.)
