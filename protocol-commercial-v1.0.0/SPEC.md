# CommandLayer Protocol — Commercial (Spec Stub)

- JSON Schema Draft 2020-12
- Strict validation
- Canonical verbs with one alias
- Requests must include: `x402`, `actor`, `trace`, `idempotencyKey`, `input`
- Receipts must include: `x402`, `actor`, `trace`, `status`, `result`

Refer to protocol-commons for baseline envelope semantics.
